defmodule Day2 do
  def part1 do
    _lines = File.read!(System.argv() |> hd()) |> String.trim() |> String.split("\n")
  end

  def part2 do
    _lines = File.read!(System.argv() |> hd()) |> String.trim() |> String.split("\n")
  end
end
